#include <stdio.h>
#include "../include/catalogo.h"

/*
 * Responsable: Felipe
 * TODO: leer el archivo de entrada (formato a definir, ver data/contracts/)
 * y llenar la estructura Catalogo.
 */
int cargarCatalogo(const char *rutaArchivo, Catalogo *catalogo) {
    (void)rutaArchivo;
    catalogo->cantidadCursos = 0;
    /* TODO: implementar carga real */
    return 0;
}
