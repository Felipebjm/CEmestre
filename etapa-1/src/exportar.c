#include <stdio.h>
#include "../include/exportar.h"

/*
 * Responsable: Neto
 * TODO: serializar 'catalogo' a JSON usando cJSON (o la librería que el
 * equipo decida) siguiendo el esquema de data/contracts/output_ejemplo_c.json
 */
int exportarCatalogo(const char *rutaArchivo, const Catalogo *catalogo) {
    (void)rutaArchivo;
    (void)catalogo;
    /* TODO: implementar */
    return 0;
}
